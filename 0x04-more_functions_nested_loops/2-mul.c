#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 *
 * Description: product of two integer
 * Return: the product
 */
int mul(int a, int b)
{
	int p;

	p = a * b;

	return (p);
}
