#include <stdio.h>

/**
 * _isupper - checks for upper or lower case
 * @c: Function accepts an int as input
 *
 * Description: Check case
 * Return: 0 or 1
 */
int _isupper(int c)

{
	while (c >= 65 && c <= 90)

		return (1);

	while (c >= 97 && c <= 122)

		return (0);
return (0);
}
