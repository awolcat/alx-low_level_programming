#include <stdio.h>
/**
 * set_string - set value of a pointer to a char
 * @s: the pointer
 * @to: the char
 * Description: check fn
 * Return: void
 */
void set_string(char **s, char *to)
{
	s = &to;
}
